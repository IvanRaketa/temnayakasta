{
  "note": "If package.json already exists in the repository, copy these scripts there.",
  "scripts": {
    "start": "npx serve .",
    "check": "node scripts/check-release.mjs",
    "zip": "node scripts/make-zip.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "serve": "^14.2.4"
  }
}
